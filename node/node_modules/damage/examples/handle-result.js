var Damage = require('../');

Damage.prepare(function () {
    var fs = require('fs');
});

var damageOf = Damage();

damageOf('a lot of calculations',function () {
  
  for(var i=10000000;i<100000000;i++)
  	Math.round(Math.log(i)*Math.sin(i)*Math.sqrt(i));

  done();
  
},10)

.is(function (processed,result) {
	console.log(result);
});