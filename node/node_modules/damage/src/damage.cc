#include <node.h>
#include <v8.h>
#include <errno.h>
#include "win.h"

using namespace v8;

void Init(Handle<Object> target) {
  target->Set(String::NewSymbol("cpuTime"), FunctionTemplate::New(CPUTime)->GetFunction());
}

NODE_MODULE(damage, Init);

